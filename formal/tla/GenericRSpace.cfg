\* TLC Configuration for GenericRSpace
\* Run with: tlc GenericRSpace.tla -config GenericRSpace.cfg

SPECIFICATION Spec

\* Model values for finite model checking
CONSTANTS
    Channels = {c1, c2, c3}
    Patterns = {p1, p2}
    Data = {d1, d2, d3}
    Continuations = {k1, k2}
    NULL = NULL

\* Check these invariants
INVARIANTS
    TypeInvariant
    NoPendingMatch

\* Check these properties
PROPERTIES
    Safety
